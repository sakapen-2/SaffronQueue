apiVersion: v1
kind: Service
metadata:
  name: {{ include "saffronqueue.fullname" . }}
  labels:
    {{- include "saffronqueue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "saffronqueue.selectorLabels" . | nindent 4 }}
